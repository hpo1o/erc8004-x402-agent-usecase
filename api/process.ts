// Root-level Vercel compatibility entrypoint.
// The canonical implementation lives under frontend/, which remains the
// recommended Root Directory for new Vercel projects.
export { default, maxDuration } from "../frontend/api/process.js";
